module.exports.cars (); {
    constructor ( color, convertable); {
        color = color
        convertable = convertable
        speed = 0
    }

    accelerate (number); {
        speed =+ number
    }

    decelerate (number); {
        speed =- number
    }
}